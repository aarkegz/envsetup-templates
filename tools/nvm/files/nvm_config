# {# Generated by envsetup - nvm config file #}
# NVM is installed via official script
# No additional configuration files needed

# To use nvm after installation, add to your shell:
# export NVM_DIR="$HOME/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"